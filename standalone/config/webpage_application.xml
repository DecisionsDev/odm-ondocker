<server description="webpage">

  <library id="config">
    <folder dir="${server.config.dir}" />
  </library>

  <application type="war" id="webpage" location="${server.config.dir}/apps/webpage.war" name="/">
    <classloader privateLibraryRef="config" />
  </application>
</server>
